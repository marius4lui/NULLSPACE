# Private native input fallback — 2026-09-04

QA-003 correction encountered an observed platform failure: the implementer reports private Xwayland24.1.13 segmentation fault after disabling four physical slave devices, with inventories/backtrace retained in native-seatfix development evidence. No host devices were changed. The failed mechanism does not justify accepting contaminated controls or weakening native validation.

Root researched an alternative boundary, not a production implementation: use a private headless Weston compositor, then private Xwayland/XTEST inside it, disconnected from the host physical seat. Weston documents a headless backend without input/output, and selectable GL/Vulkan/Pixman compositor renderers. A maintained change adds an optional fake seat for Xwayland compatibility. These are candidate capabilities, not proof this host configuration works. [Official Weston running documentation](https://wayland.pages.freedesktop.org/weston/toc/running-weston.html), [official fake-seat change](https://cgit.freedesktop.org/wayland/weston/commit/?id=0126a5b4fc03c3ac7f023d87303bf09f7d494ac1).

Implementer must inspect actual installed version/help and verify isolated socket/process ownership, no physical-input backend, actual application Forward+/RADV, XTEST control/captured mouse, game-only capture/audio and cleanup. A software compositor does not by itself identify application rendering; no silent llvmpipe substitution. Independent reviewer evaluates the isolation property rather than mandating one faulty XI2 mechanism. Goal/gates unchanged.
