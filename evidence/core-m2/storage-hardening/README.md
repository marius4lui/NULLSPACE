# Independent-audit storage corrections

Bounded implementer validation, not independent acceptance or finished M2. Parent source is `68ce558`; separate reviewer `/root/bootstrap_m0` reported the defects at review commit `3815bdd` (probe source `3bc88be`). Its original 741-substitution log/report remain in `agent/review-core-storage`, `evidence/reviews/core-storage/`. No earlier M2 evidence was overwritten. Shared-clock work was shelved while this distinct correction was built.

Corrections: check quality is a String before typed-array lookup; recognize nonnegative integral version metadata without the arbitrary 100000 ceiling or an overflowing integer conversion; deep-copy mutable values returned by SettingsManager.get_value. Save schema remains version 1. Unknown version reads/writes fail non-destructively, including when an older valid backup exists. Numeric JSON interpretation is Godot's: finite integral values are supported for metadata comparison without claiming arbitrary-precision arithmetic.

Actual Godot 4.7.2.stable.official.ed1daf0bf. The extended suite preserves all previous 80 checks and adds 71 checks: 13 malformed quality variants; mutable-resolution isolation; and both version fields at 999, 100000, 100001, 2147483647, int64 maximum, 1e30 and 1e300, protecting primary and backup bytes. `before-fix.log` is the original failing engine log: 151 checks, 32 failed assertions plus the typed quality engine errors. `after-fix.log`: 151 checks, zero failures, no SCRIPT ERROR/engine ERROR. The error-aware wrapper returns nonzero for the failing run.

Commands: first import with `GODOT --headless --path game --editor --import --quit`; then `bash game/tests/core/run.sh GODOT ABSOLUTE_LOG`. The suite creates a fresh XDG profile and separate temporary storage. After correction another import, both `--export-release` presets and Linux packed-export `--headless --quit-after 3` all ran without engine/script errors (`import.log`, `export-linux.log`, `export-windows.log`, `linux-headless.log`). Build/packed-run used a new isolated XDG profile with a symlink to the verified existing matching export templates. No native play, native Windows execution or visual/audio quality claim is made.

Generated milestone binaries were exported under `build/m2-storage-hardening/` and then moved intact to a task-specific `/tmp/nullspace-m2-storage-hardening.*/exports/` directory; they are not committed release artifacts. Linux SHA256 `80206f8e6be41e985107c14c1c998876d0e58cf797791096f3271fe6f92a0d60`; Windows SHA256 `e8fb16fca09e355ee18263e774f2bb2560629d4c34a49011fad3aed731a4e463`. Source was the parent plus this commit's storage/test changes; logs were captured before commit. Independent exact-source rerun is requested; whole-M2 native self-validation and separate acceptance remain pending corrected QA infrastructure.
